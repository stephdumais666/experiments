/*
import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders shopify gallery", () => {
  render(<App />);

  const linkElement = screen.getByText(/book/i);
  expect(linkElement).toBeInTheDocument();
});
*/
